import React, { Component } from 'react'

class Material extends Component {
  constructor (props) {
    super(props)
  }

  render () {
    return (
      <section className='Material'>
        <h1>Current Subject: </h1>
      </section>
    )
  }
}
// Next page user will see

  // Need either to set material to vote on

  // Or choose one that's already in system

export default Material
